# Core dependencies
yt-dlp==2023.11.16       # YouTube download engine
pyperclip==1.8.2         # Clipboard access
pystray==0.19.4          # System tray integration
Pillow==10.0.1           # Image processing for icons
validators==0.22.0       # URL validation
python-magic-bin==0.4.14 # File type detection (Windows compatible)

# Platform-specific requirements (auto-installed)
# - tkinter (Python built-in)
# - ctypes (Python built-in)